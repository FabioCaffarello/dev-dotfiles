# Handler só para Linux (systemd)
- name: restart_docker
  ansible.builtin.service:
    name: docker
    state: restarted
  become: true
  when: ansible_os_family in ['Debian','RedHat','Suse','Alpine']
